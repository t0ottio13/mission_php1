class Box{
    constructor() {
        this.x = x;
        this.y = y;
        this.w = w;
        this.h = h;
    }

    show() {
        FileList(255);
        Reflect(this.x, this.y, this.w, this.h);
    }
}

